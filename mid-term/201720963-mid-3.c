#include <stdio.h>
#include <string.h>


int main()
{
    char str[20];
    
    while(scanf("%s", str[i++]) != EOF)








    char tmp[80];
    
    if(strcmp(str[j], str[j+1] > 0) {
        strcpy(tmp, str[j]);
        strcpy(str[j], str[j+1]);
        strcpy(tmp, str[j]);
    }


